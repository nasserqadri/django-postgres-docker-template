# django-postgres-docker-template

#  Docker template for Django appllications

This project contains the requisite docker files to create a Django application with a postgres database, and relevant mounted volumes on localhost. I use this structure to build backend applications with Django. 